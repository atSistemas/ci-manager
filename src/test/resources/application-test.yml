server.port: 8080

logging:
#  file: logs/ci-manager.log
  level:
    org.springframework.web: info
    com.atsistemas: debug

gogs:
  user: gogs
  passwd: gogs
  migrateContextPath: /api/v1/repos/migrate
  migrateUrl: 'http://gogs.cicd.svc:3000${gogs.migrateContextPath}'
  
openshift:
  url: 'https://localhost:8443'
  user: developer
  passwd: any